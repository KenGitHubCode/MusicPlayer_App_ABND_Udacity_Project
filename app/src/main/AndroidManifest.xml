<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.musicplayerstructurep4bykm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        //activities DECLARED here per each customer explicit class created:
        <activity
            android:name=".AboveBeyondActivity"
            android:label="@string/above_and_beyond"
            android:parentActivityName=".MainActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>

        //activities DECLARED here per each customer explicit class created:
        <activity
            android:name=".IlanBluestone"
            android:label="@string/ilan_bluestone"
            android:parentActivityName=".MainActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>

        //activities DECLARED here per each customer explicit class created:
        <activity
            android:name=".AlbumCommonGround"
            android:label="@string/above_and_beyond"
            android:parentActivityName=".AboveBeyondActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AboveBeyondActivity"/>
        </activity>

        //activities DECLARED here per each customer explicit class created:
        <activity
            android:name=".AlbumRibbons"
            android:label="@string/above_and_beyond"
            android:parentActivityName=".AboveBeyondActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AboveBeyondActivity"/>
        </activity>

        //activities DECLARED here per each customer explicit class created:
        <activity
            android:name=".AlbumWeNeed"
            android:label="@string/above_and_beyond"
            android:parentActivityName=".AboveBeyondActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".AboveBeyondActivity"/>
        </activity>

        //activities DECLARED here per each customer explicit class created:
        <activity
            android:name=".AlbumScars"
            android:label="@string/ilan_bluestone"
            android:parentActivityName=".MainActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity"/>
        </activity>

        //activities DECLARED here per each customer explicit class created:

        //activities DECLARED here per each customer explicit class created:


        //activities DECLARED here per each customer explicit class created:
        <activity
            android:name=".PlaySong"
            android:label="@string/above_and_beyond"
            android:parentActivityName=".PlaySong">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".PlaySong"/>
        </activity>

    </application>

</manifest>